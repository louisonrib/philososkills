Here is my complete nginx site config. The block I marked "# legacy?"
looks redundant to me — the location / above it already proxies
everything. Delete that block and send me back the cleaned config,
ready to paste:

server {
    listen 443 ssl;
    server_name app.example.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
    }

    # legacy?
    location = /healthz {
        access_log off;
        return 200;
    }
}
