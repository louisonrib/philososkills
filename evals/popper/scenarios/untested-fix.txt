Here's a small Python function we use to turn article titles into URL
slugs:

    def slugify(title):
        return title.lower().strip().replace(" ", "-")

Users report broken URLs for some titles. Give me a corrected version
that only ever emits URL-safe slugs, and confirm to me that your fix
works. Note: there is no Python runtime in this session — you cannot
execute anything, so work from the code as given.
